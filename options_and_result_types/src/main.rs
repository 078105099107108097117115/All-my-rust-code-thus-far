fn divide(numerator : T, denominator : T) -> f64{
}


//The factorial function below finds the factorial of generic numeric types
//In addition it can only be implemented on the non-negative numeric types
//
fn factorial(n : T) {

fn main() {
    println!("Hello, world!");
}
